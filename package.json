{
    "name": "vscode-ext-vim-save",
    "displayName": "vscode-ext-vim-save",
    "description": "",
    "version": "0.0.1",
    "publisher": "kanonji",
    "engines": {
        "vscode": "^1.0.0"
    },
    "categories": [
        "Other"
    ],
    "contributes": {
        "commands": [{
            "command": "workbench.action.files.save",
            "title": ":w (Save)"
        }]
    },
    "scripts": {
        "vscode:prepublish": "node ./node_modules/vscode/bin/compile",
        "compile": "node ./node_modules/vscode/bin/compile -watch -p ./",
        "postinstall": "node ./node_modules/vscode/bin/install"
    },
    "devDependencies": {
        "typescript": "^1.8.5",
        "vscode": "^0.11.0"
    }
}